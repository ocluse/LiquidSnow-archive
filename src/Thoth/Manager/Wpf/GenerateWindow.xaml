<Window x:Class="Thismaker.Thoth.Manager.Wpf.GenerateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Thismaker.Thoth.Manager.Wpf"
        xmlns:goro="http://schemas.thismaker.com/liquidsnow/goro"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
         Height="auto" Width="auto" ResizeMode="NoResize"
        Title="Generate Classes" Style="{DynamicResource StandardWindow}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBox x:Name="txtNamespace" goro:ControlExtensions.Header="Namesapce" Width="400" Margin="8"/>
            <TextBox x:Name="txtOutputDir" goro:ControlExtensions.Header="Output Directory" Width="400" Margin="8"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Browse" Width="100" Margin="4" Click="OnBrowse"/>
            <Button Content="Generate" Width="100" Margin="4" Click="OnGenerate"/>
        </StackPanel>
    </Grid>
</Window>
